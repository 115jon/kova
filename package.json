{
  "name": "ralph-auth",
  "private": true,
  "scripts": {
    "dev:server": "pnpm --filter server dev",
    "dev:dashboard": "pnpm --filter dashboard dev",
    "deploy:server": "pnpm --filter server deploy",
    "deploy:dashboard": "pnpm --filter dashboard deploy",
    "migrate:local": "pnpm --filter server migrate:local",
    "migrate:prod": "pnpm --filter server migrate:prod"
  }
}
