# ⚠ DEPRECATED — This file is no longer the authoritative Wrangler configuration.
#
# The single source of truth for all wrangler config, bindings, routes, D1, KV,
# Durable Objects, and deploy settings is:
#
#   dashboard/wrangler.toml
#
# This file is kept here to avoid breaking any IDE integrations or tooling that
# might expect a wrangler.toml next to the TypeScript source, but running
# `wrangler dev` or `wrangler deploy` from this directory will NOT produce a
# correct result (missing [assets], wrong paths, no migrations, etc.).
#
# ── Correct commands (always run from repo root or dashboard/) ────────────────
#   pnpm dev              →  starts Vite + Miniflare on :5174
#   pnpm migrate:dev      →  applies pending D1 migrations to local DB
#   pnpm migrate:prod     →  applies migrations to production D1
#   pnpm deploy           →  build + wrangler deploy
#   pnpm cf-typegen       →  regenerate Cloudflare worker types
# ─────────────────────────────────────────────────────────────────────────────

name = "kova-auth-server"
main = "src/index.ts"
compatibility_date = "2026-04-12"
compatibility_flags = ["nodejs_compat"]

# DO NOT ADD BINDINGS HERE. See dashboard/wrangler.toml.
